# ลบบัญชีในแอป — งานที่เสร็จแล้ว และขั้นตอนที่เหลือ

> ทำเมื่อ 17 ส.ค. 2026 · ปิดตัวบล็อกข้อ 0.1 ของ `gtm-q4-2026/02-สเปกงานเทคนิค`
> **build ผ่านแล้ว** (`Compiled successfully.`) และตรวจว่าโค้ดใหม่ติดไปใน bundle จริง

---

## เสร็จแล้ว

| ไฟล์ | ทำอะไร |
|---|---|
| `supabase/functions/delete-account/index.ts` | 🆕 Edge Function ลบข้อมูลทุกตาราง แล้วลบ auth user ด้วย service role |
| `src/services/SupabaseService.js` | 🆕 `deleteAccount()` + `clearLocalUserData()` |
| `src/pages/settings.js` | 🆕 แถว "ลบบัญชีถาวร" ในหมวดบัญชีผู้ใช้ + ยืนยัน 3 ชั้น |
| `src/i18n/th.js` · `src/i18n/en.js` | 🆕 คีย์ `deleteAccount*` 11 คีย์ ตรงกันทั้งสองภาษา |
| `.gitignore` | เพิ่ม `build-verify/` |

`public/delete-account.html` **มีอยู่แล้ว** (349 บรรทัด) — เหลือแค่ deploy

### การตัดสินใจเชิงออกแบบที่ควรรู้

**ทำไมต้องเป็น Edge Function ไม่ใช่โค้ดในแอป**
การลบ auth user ต้องใช้ `service_role` key ซึ่งมีสิทธิ์ข้าม RLS ทั้งฐานข้อมูล ถ้าฝังในแอป ใครถอด APK ก็ได้คีย์ไปคุมข้อมูลผู้ใช้ทุกคน — ตรวจแล้วว่า **ไม่มี `SERVICE_ROLE` หลุดเข้า bundle**

**ลำดับการลบ: ข้อมูลก่อน → auth user ทีหลัง**
ถ้าลบ auth user ก่อนแล้วขั้นลบข้อมูลพัง จะไม่มี JWT ให้ลองใหม่ได้อีก และข้อมูลจะกลายเป็นกำพร้าที่ไม่มีใครลบได้ ฟังก์ชันจึงหยุดทันทีถ้าลบตารางไหนไม่สำเร็จ (คืน 500) โดยยังไม่แตะ auth user

**ตัวตนมาจาก JWT ไม่ใช่จาก body**
`userId` อ่านจาก token ที่ผู้ใช้ส่งมา ไม่ได้รับ id จาก request body — ผู้ใช้จึงลบได้เฉพาะบัญชีตัวเอง ปลอมเป็นคนอื่นไม่ได้

**ล้างข้อมูลในเครื่องหลังเซิร์ฟเวอร์ยืนยันแล้วเท่านั้น**
ถ้าล้างก่อนแล้วลบไม่สำเร็จ ผู้ใช้จะเสียข้อมูลในเครื่องฟรี ๆ ทั้งที่บัญชียังอยู่ · เก็บ `language` กับ `darkMode` ไว้เพราะเป็นการตั้งค่าอุปกรณ์ ไม่ใช่ข้อมูลส่วนบุคคล

**เตือนเรื่องสมาชิกที่ยังใช้งานอยู่**
ถ้า `plan !== 'free'` จะเด้งเตือนว่า **การลบบัญชีไม่ได้ยกเลิกการเรียกเก็บเงิน** ต้องไปยกเลิกที่หน้าสมาชิกของ Google Play เอง — ถ้าไม่เตือน ผู้ใช้จะถูกตัดเงินต่อทั้งที่บัญชีหายแล้ว ซึ่งจบที่รีวิว 1 ดาวและ chargeback

---

## 🔴 ต้องทำเองต่อ (เรียงตามลำดับ)

### 1. ลบไฟล์ที่ผมลบเองไม่ได้

สภาพแวดล้อมที่ผมทำงานอยู่ลบไฟล์บนเครื่องคุณไม่ได้ (สั่ง `rm` แล้วได้ `Operation not permitted`) เหลือ 2 อย่าง:

```bash
cd ~/projects/PFM
rm -f .git/index.lock      # ล็อกค้างจาก git process เก่า — ทำให้ commit ไม่ได้
rm -rf build-verify        # โฟลเดอร์ที่ผมสร้างไว้ทดสอบ build (ใส่ .gitignore แล้ว)
```

### 2. Deploy Edge Function

```bash
cd ~/projects/PFM
supabase functions deploy delete-account
```

> `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` ถูกฉีดให้อัตโนมัติ ไม่ต้องตั้งเอง

**ตรวจว่า deploy แล้วใช้ได้:**
```bash
curl -i -X POST "https://<project-ref>.supabase.co/functions/v1/delete-account" \
  -H "Content-Type: application/json" -d '{}'
# ต้องได้ 401 Unauthorized  ← ถูกต้อง แปลว่าฟังก์ชันขึ้นแล้วและกันคนไม่มี token
```

### 3. ตรวจตารางให้ครบ

`USER_TABLES` ในฟังก์ชันตั้งไว้ตามสคีมาที่เจอในโปรเจกต์:

```
sync_history · error_logs · budget_limits · transactions
user_ai_usage · subscriptions · user_profiles
```

รันใน Supabase SQL Editor เพื่อดูว่ามีตารางไหนตกหล่นไหม:

```sql
select table_name from information_schema.columns
where table_schema = 'public' and column_name = 'user_id'
order by table_name;
```

ถ้าเจอตารางที่ไม่อยู่ในลิสต์ **ต้องเพิ่มเข้า `USER_TABLES`** ไม่งั้นจะเหลือข้อมูลกำพร้า ซึ่งผิดข้อกำหนดของ Google

> ตารางที่ยังไม่มีจริงในโปรเจกต์ ฟังก์ชันจะข้ามให้เอง (`skipped`) ไม่พัง

### 4. Deploy หน้าเว็บ

`public/delete-account.html` ต้องเปิดได้แบบสาธารณะ **โดยไม่ต้องล็อกอิน** แล้วเอา URL ไปใส่ใน
Play Console → **App content → Data safety → Account deletion URL**

### 5. ทดสอบก่อนปล่อย

| # | เคส | ผลที่ต้องได้ |
|---|---|---|
| 1 | สมัครบัญชีทดสอบ → เพิ่มรายการ 3 รายการ + งบ 1 หมวด | มีข้อมูลใน Supabase |
| 2 | Settings → ลบบัญชี → กด Cancel ที่ prompt | ไม่มีอะไรถูกลบ |
| 3 | พิมพ์คำยืนยันผิด | ขึ้น "ข้อความยืนยันไม่ตรง" และไม่ลบ |
| 4 | พิมพ์ `ลบบัญชี` (หรือ `DELETE` ถ้าใช้ EN) ให้ถูก | ลบสำเร็จ + เด้งกลับหน้าแรก |
| 5 | เช็คทุกตารางใน Supabase | ไม่เหลือแถวของ user นั้นเลย |
| 6 | Authentication → Users | ไม่มี user นั้นแล้ว |
| 7 | ล็อกอินด้วยอีเมลเดิม | เข้าไม่ได้ |
| 8 | สลับเป็นภาษาอังกฤษแล้วลบ | ข้อความเป็นอังกฤษทั้งหมด คำยืนยันคือ `DELETE` |
| 9 | บัญชีที่มีสมาชิก Pro | ต้องเจอคำเตือนเรื่องยกเลิกที่ Play ก่อน |
| 10 | ตัดเน็ตแล้วกดลบ | ขึ้น error ไม่ลบข้อมูลในเครื่อง |

> ⚠️ **ข้อ 10 สำคัญที่สุด** — เป็นเคสที่ทำให้ผู้ใช้เสียข้อมูลฟรีถ้าเขียนผิดลำดับ

---

## งานถัดไปตามลำดับในสเปก

| # | งาน | สถานะ |
|---|---|---|
| 0.1 | ลบบัญชีในแอป | ✅ **เสร็จ** (เหลือ deploy + ทดสอบ) |
| 0.2 | หน้าลบบัญชีบนเว็บ | ⏳ ไฟล์มีแล้ว เหลือ deploy |
| 0.3 | RevenueCat API key จริง | ⏳ **คุณต้องทำเอง** (เป็นความลับ ผมไม่ควรแตะ) · ผูก service account ด้วย — Google ใช้เวลาถึง 36 ชม. **ทำวันนี้** |
| 0.4 | รวม `NATIVE_BILLING_READY` เป็นค่าเดียว | ⏳ พร้อมทำต่อ (5 ไฟล์) |
| 3.1 | เปลี่ยนราคารายปี 990 → 1,188 | ⏳ พร้อมทำต่อ |
| 3.2 | `src/config/promo.js` + แบนเนอร์นับถอยหลัง | ⏳ พร้อมทำต่อ |
| 3.3 | แก้ราคา hardcode `฿` → `priceString` จากร้าน | ⏳ พร้อมทำต่อ |
| 3.4 | Paywall เหลือ 3 ใบ | ⏳ พร้อมทำต่อ |
| 3.6 | ปุ่มกู้คืนการซื้อ | ⏳ พร้อมทำต่อ |
